-- [폐기됨] 2단계 Unsplash 전용 방식에서 사용하던 컬럼.
-- 3단계(섹션별 visual 방식)로 전환하면서 body_sections[].visual.photo 로 대체됨.
-- 실행 불필요 — 참고용으로만 보관.
--
-- alter table newsletter_issues
--   add column if not exists body_image_url text,
--   add column if not exists body_image_credit text,
--   add column if not exists body_image_credit_link text;
